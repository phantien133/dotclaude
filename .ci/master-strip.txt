# Dev-only source dirs — present on develop, absent on master
claudekit/
presets/
scripts/
upstream/

# Build tooling
node_modules/
tsconfig.json
vitest.config.ts
pnpm-lock.yaml

# Dev docs and metadata
# NOTE: leading slash anchors to repo root so plugin-level docs (e.g.
# plugins/<name>/AGENTS.md) are NOT stripped — only the root dev-meta files.
/CLAUDE.md
/AGENTS.md
/dependencies.yaml
.claude/

# CI config (not needed on distribution branch)
.github/
.ci/

# rsync internals — no trailing slash: matches both .git/ dir (source) and .git file (worktree)
.git
