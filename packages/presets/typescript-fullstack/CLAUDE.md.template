# CLAUDE.md

> Project context cho Claude Code. Render từ dotclaude preset `typescript-fullstack`. Tùy chỉnh tự do.

## Stack

- Language: TypeScript (strict mode)
- Frontend: <Next.js / React / ...>
- Backend: <NestJS / Express / Fastify / ...>
- Database: <Postgres + Prisma / Drizzle / ...>
- Package manager: <pnpm / npm / yarn / bun>
- Test: <Vitest / Jest / Playwright>

## Cấu trúc repo (TODO: cập nhật)

```
src/
├── app/              # Next.js app router (nếu có)
├── components/
├── lib/
├── server/
└── ...
```

## Conventions

- Strict TypeScript: `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`
- ESLint + Prettier qua hook (xem `.claude/settings.json`)
- Test placement: cạnh source (`*.test.ts`) hoặc `__tests__/`
- Commit: conventional commits (`feat`, `fix`, `chore`, …)

## Skills loaded

Xem `.claude/skills/` — preset cài sẵn:
- `coding-standards`, `frontend-patterns`, `backend-patterns`, `api-design`, `mcp-server-patterns`, `nestjs-patterns`

Skills load on-demand qua trigger keyword/path, không tốn context khi không dùng.

## Workflow gợi ý

1. Bắt đầu task: gọi `/plan` để Claude lên kế hoạch.
2. Implement với `/tdd` (write tests first).
3. Code review với `/code-review` trước commit.
4. Build/type check tự động qua hooks.

## Project-specific notes

<!-- Thêm context riêng của repo: domain, business rules, kiến trúc đặc biệt, gotchas -->
