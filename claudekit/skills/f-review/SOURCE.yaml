# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# Vendored from upstream/dotclaude (components branch) — self-authored component library.
# Source of truth: upstream/dotclaude/figma/skills/f-review/
source:
  repo: https://github.com/phantien133/dotclaude
  commit: f2f3be0233f15845decc89b79be8128a8ea25d84
  path: figma/skills/f-review
  ref: components
imported_at: "2026-05-13"
license: MIT
modified: false
modifications: null
notes: >-
  Figma design vs implementation diff. Compares current Figma state (via MCP) or
  a new export folder against installed components from the latest manifest.
  Generates per-component change report (new props, removed variants, token drift)
  and applies updates selectively — preserving local customizations.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands:
      - f-setup
    hooks: []
    rules: []
  external:
    - mcp__figma__get_file_components
    - mcp__figma__get_local_variables
    - mcp__figma__get_file_nodes
tags:
  - figma
  - ui
  - review
  - diff
  - update
categories:
  purpose:
    - ui-integration
    - figma
    - review
  mechanism:
    - diff
    - interactive
