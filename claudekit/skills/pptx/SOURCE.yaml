# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
source:
  repo: https://github.com/anthropics/skills
  commit: d211d437443a7b2496a3dad9575e7dddd724c585
  path: skills/pptx
  ref: main
imported_at: "2026-05-08"
license: Proprietary
modified: false
modifications: null
notes: >-
  Create, read, edit, or manipulate .pptx files. Includes editing.md,
  pptxgenjs.md, and scripts/. Proprietary Anthropic license — personal use
  only, do not redistribute.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  external:
    - name: markitdown
      type: python_pkg
      version: "[pptx]"
      reason: "SKILL.md: text extraction from .pptx — `pip install 'markitdown[pptx]'`"
    - name: defusedxml
      type: python_pkg
      reason: "scripts/clean.py, thumbnail.py"
    - name: Pillow
      type: python_pkg
      reason: "scripts/thumbnail.py — PIL.Image, ImageDraw, ImageFont"
    - name: soffice
      type: system_binary
      reason: "scripts/thumbnail.py + SKILL.md: LibreOffice for PDF conversion"
tags:
  - pptx
  - presentations
  - office
categories:
  purpose:
    - documents
  stack:
    - cross-stack
