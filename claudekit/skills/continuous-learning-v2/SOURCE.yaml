# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
source:
  repo: https://github.com/affaan-m/everything-claude-code
  commit: 841beea45cb25ba51f29fa45b7e272938d19b80a
  path: skills/continuous-learning-v2/SKILL.md
  ref: main
imported_at: 2026-05-08
license: MIT
modified: true
modifications: "config.json: observer.enabled set to true (was false upstream)"
notes: >-
  Instinct-based learning system v2.1. Hooks observe every tool call,
  background observer (Haiku) clusters patterns into atomic instincts.
  Project-scoped by default (git remote hash).
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  external:
    - name: python3
      type: system_binary
      reason: "observe.sh and scripts/instinct-cli.py require Python 3"
