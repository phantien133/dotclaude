# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# This is a project-native skill (not vendored from upstream). The repo/commit
# below point to dotclaude itself at the commit when the skill was first created.
source:
  repo: https://github.com/phantien133/dotclaude
  commit: 4b6cdef1a8daa7411670036a2ff4596060c03a0f
  path: claudekit/skills/dotclaude-component-picker
  ref: main
imported_at: "2026-05-08"
license: MIT
modified: true
modifications: |
  - Added references/ directory: sidecar.md (full schema, layout rules, dep resolution,
    modify/upgrade workflows) and presets.md (preset schema, extends, settings_patch,
    install flags).
  - SKILL.md v2: added bundled reference pointers, tags/categories in sidecar template,
    modify-after-import workflow, pnpm sync upgrade path, --force --symlink install flags,
    "Upgrade an existing component" section, richer Step 2 (eval) and Step 6 (install).
  - SKILL.md v3 (skill-creator eval loop, 2026-05-08): added Step 2.5 Cross-Reference &
    Dependency Scan — systematic grep scan, decision matrix (vendor/reuse/external), shared-code
    pattern guidance, existing-dep verification (Step 2.5d). Pipeline 7→8 steps. evals.json
    extended to 6 cases including cross-reference scenarios. Benchmark: 100% vs 50% old skill.
notes: >-
  Project-native skill created in step 5 of the continuous-learning-v2 plan
  (session 2026-05-08), improved via skill-creator in the same session.
  Encodes the full dotclaude component pick/vendor/install pipeline with
  bundled references for sidecar and preset schemas. Key pitfall: pnpm
  install intercepts `pnpm install <preset>` as npm install — always use
  pnpm install:project or install:user with --symlink --force.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  external: []
tags:
  - workflow
  - vendoring
  - component-management
  - dotclaude
categories:
  purpose:
    - workflow
    - installation
  mechanism:
    - guided-steps
