# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# Vendored from upstream/dotclaude (components branch) — the self-authored component library.
# Source of truth: upstream/dotclaude/dotclaude-self/skills/plugin-discovery/
source:
  repo: https://github.com/phantien133/dotclaude
  commit: 8b40c89f165e1afb41795b58ef97551217560b1c
  path: dotclaude-self/skills/plugin-discovery
  ref: components
imported_at: "2026-05-13"
license: MIT
modified: false
modifications: null
notes: >-
  Self-authored skill, source of truth in upstream/dotclaude branch components.
  Searches GitHub (auto) and optionally npm (user opt-in) for external Claude Code
  components to vendor into claudekit/. Quality filter: ≥50 stars minimum, active
  within 12 months, MIT/Apache-2.0 license, recognisable component structure.
  Warns clearly when no qualifying candidates are found. Vendor path: git submodule
  into upstream/<alias> + dependencies.yaml update + delegates to
  dotclaude-component-picker for the actual pick/sidecar/install pipeline.
  Scope strictly limited to dotclaude vendoring — not for general search tasks.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills:
      - dotclaude-component-picker
    commands: []
    hooks: []
    rules: []
  external: []
tags:
  - search
  - vendoring
  - component-management
  - dotclaude
categories:
  purpose:
    - preset-authoring
    - discovery
  mechanism:
    - guided-steps
    - interactive
