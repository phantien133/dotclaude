# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# Vendored from upstream/dotclaude (components branch) — self-authored component library.
# Source of truth: upstream/dotclaude/workflow/skills/w-checkpoint/
source:
  repo: https://github.com/phantien133/dotclaude
  commit: f2f3be0233f15845decc89b79be8128a8ea25d84
  path: workflow/skills/w-checkpoint
  ref: components
imported_at: "2026-05-13"
license: MIT
modified: false
modifications: null
notes: >-
  TDD checkpoint commit helper. Args: RED|GREEN <task-slug>. Stages affected files
  from impact.md, creates a conventional commit (test: RED/GREEN marker), and
  updates state.yaml with checkpoint hash. Invoked by w-task Phase 4 — stays as
  a skill (not command) so it can be called programmatically.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  external: []
tags:
  - workflow
  - tdd
  - checkpoint
  - git
categories:
  purpose:
    - tdd
    - dev-workflow
  mechanism:
    - git
    - commit
