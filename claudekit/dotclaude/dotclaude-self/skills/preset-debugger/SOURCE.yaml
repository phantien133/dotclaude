# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# Vendored from upstream/dotclaude (components branch) — the self-authored component library.
# Source of truth: upstream/dotclaude/dotclaude-self/skills/preset-debugger/
source:
  repo: https://github.com/phantien133/dotclaude
  commit: 8b40c89f165e1afb41795b58ef97551217560b1c
  path: dotclaude-self/skills/preset-debugger
  ref: components
imported_at: "2026-05-13"
license: MIT
modified: false
modifications: null
notes: >-
  Self-authored skill, source of truth in upstream/dotclaude branch components.
  Diagnoses and fixes failures in dotclaude preset artifacts (YAML, sidecars,
  plugin builds, vitest stubs). Called from /preset-wizard on verify failure, or
  manually by the user. Scope strictly limited to dotclaude preset/plugin artifacts.
  Handles 6 known failure patterns: schema violation, component not found, sidecar
  validation, TS errors, vitest failures, and plugin build crashes.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills:
      - dotclaude-component-picker
    commands: []
    hooks: []
    rules: []
  external: []
tags:
  - debugging
  - preset
  - workflow
  - dotclaude
categories:
  purpose:
    - troubleshooting
    - preset-authoring
  mechanism:
    - guided-steps
    - interactive
