# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# Vendored from upstream/dotclaude (components branch) — self-authored component library.
# Source of truth: upstream/dotclaude/figma/skills/f-ui-kit/
source:
  repo: https://github.com/phantien133/dotclaude
  commit: 59b24718803df9a85cdf1914a4225d3978520dd2
  path: figma/skills/f-ui-kit
  ref: components
imported_at: "2026-05-13"
license: MIT
modified: false
modifications: null
notes: >-
  Manifest-aware Figma UI kit integration. Reads semantic roles from f-import
  manifest (shared-component / layout / playground / page) instead of filename
  heuristics. Handles HTML→React transforms, import alias rewrite, CSS module
  placement. Can also run standalone with an export folder path.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands:
      - f-setup
    hooks: []
    rules: []
  external: []
tags:
  - figma
  - ui
  - components
  - react
categories:
  purpose:
    - ui-integration
    - figma
  mechanism:
    - transform
    - file-generation
