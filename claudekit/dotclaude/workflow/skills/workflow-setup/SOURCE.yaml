# yaml-language-server: $schema=../../../presets/schema/sidecar.schema.json
#
# SOURCE.yaml is INSIDE the skill folder.
# Installer MUST exclude this file when copying the skill folder to a target.
#
# Vendored from upstream/dotclaude (components branch) — self-authored component library.
# Source of truth: upstream/dotclaude/workflow/skills/workflow-setup/
source:
  repo: https://github.com/phantien133/dotclaude
  commit: 61c49894c560e2108be439c212e22637c82fe5c6
  path: workflow/skills/workflow-setup
  ref: components
imported_at: "2026-05-13"
license: MIT
modified: true
modifications: >-
  Workflow v2 (multi-repo). Step 0 version detection vs CURRENT_VERSION=2 with Step 0a
  auto-migration-with-confirm for older configs. Step 6a configures the top-level
  repos[] list (per-repo default_branch; "." = main workspace) — a superproject may
  mix main and master. Written YAML is version: 2 with repos[].
notes: >-
  Setup wizard that writes .claude/workflow.yaml — the project config consumed
  by dev-task, quick-fix, workflow-status, workflow-reset, workflow-checkpoint,
  and create-pr. Supports GitHub Issues, Plane (with/without MCP), Jira, Linear,
  and free-text (none). Auto-detects test/typecheck/lint commands. Run once per
  project before using any workflow skill.
dependencies:
  required:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  optional:
    agents: []
    skills: []
    commands: []
    hooks: []
    rules: []
  external: []
tags:
  - workflow
  - setup
  - config
  - onboarding
categories:
  purpose:
    - setup
    - workflow
  mechanism:
    - guided-steps
    - interactive
