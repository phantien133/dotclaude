{
  "eval_id": "eval-4-pptx-shared-scripts",
  "configuration": "with_skill",
  "expectations": [
    {
      "text": "Runs a diff/comparison command to verify scripts/office/ is identical across pptx/docx/xlsx",
      "passed": true,
      "evidence": "Step 2.5 2.5c shows: `diff -rq upstream/anthropic-skills/skills/pptx/scripts/office/ \"$candidate/office/\"` looped over docx and xlsx, with results 'claudekit/skills/docx/scripts/office/ → IDENTICAL' and 'claudekit/skills/xlsx/scripts/office/ → IDENTICAL'."
    },
    {
      "text": "Correctly identifies this as upstream self-contained design (NOT a missing dependency)",
      "passed": true,
      "evidence": "Step 2.5b decision matrix row: 'scripts/office/ shared with docx, xlsx — Shared-code pattern (2.5c) — upstream design, each skill self-contained → Do NOT declare as dependency. Note in notes:.' Response explicitly says 'This is upstream design — each skill is intentionally self-contained. We do not create a shared dependency or symlink.'"
    },
    {
      "text": "Does NOT add docx or xlsx as required dependencies in SOURCE.yaml",
      "passed": true,
      "evidence": "Corrected SOURCE.yaml at end of response shows `dependencies.required.skills: []` (empty). Decision matrix explicitly says 'Do NOT declare as dependency.'"
    },
    {
      "text": "Notes the shared code pattern in SOURCE.yaml notes field for future sync awareness",
      "passed": true,
      "evidence": "Corrected SOURCE.yaml notes field reads: 'scripts/office/ (pack, validate, unpack, soffice, validators/, helpers/, schemas/) is byte-for-byte identical to claudekit/skills/docx and claudekit/skills/xlsx at the same upstream commit — this is upstream design (self-contained). Sync all three together when upgrading.'"
    },
    {
      "text": "Gives a clear direct answer to user's question (no, you don't need to pick docx/xlsx separately)",
      "passed": true,
      "evidence": "Dedicated section 'Answer to the user's question': '**No.** pptx is entirely self-contained: No references to claudekit/skills/docx/ or claudekit/skills/xlsx/ at runtime...'"
    }
  ]
}
