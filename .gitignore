# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Upstream submodule node_modules (in case ECC pulls deps locally)
upstream/everything-claude-code/node_modules/

# Private overlays (per CQ-4a — gitignored, mỗi top package có private/ riêng)
/claudekit/private/
/presets/private/
/plugins/private/

# Backup files từ installer (CQ-5c — backup-then-overwrite default)
*.bak.*

# Manifest installer (CQ-5e — track per-target, không commit chung repo)
.dotclaude-manifest.yaml

# TypeScript / Bun
node_modules/
dist/
*.tsbuildinfo

# Test scratch
.tmp/
.cache/
coverage/
